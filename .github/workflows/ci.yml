name: ci

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04   # pinned: ubuntu-latest migrates to Ubuntu 26 in Oct 2026
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Syntax check
        run: for f in server.js lib/*.js; do node --check "$f"; done
      - name: Unit + e2e tests
        run: node --test test/*.test.js

  docker:
    needs: test
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build (validate)
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: advarr:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke (container boots + health)
        run: |
          docker run -d --name advarr-smoke -p 8787:8787 advarr:ci
          for i in $(seq 1 20); do
            if curl -fsS http://127.0.0.1:8787/api/v1/health | grep -q '"ok":true'; then
              echo "healthy"; break
            fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:8787/api/v1/health
          docker rm -f advarr-smoke

      - name: Trivy security scan (report)
        # pinned by commit SHA (v0.36.0); tags like @0.28.0 no longer resolve
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          image-ref: advarr:ci
          format: json
          output: /tmp/trivy.json
          ignore-unfixed: true
          severity: HIGH,CRITICAL
          scanners: vuln
          exit-code: '0'

      - name: Enforce trivy results (annotate + fail)
        run: |
          echo "## Trivy (HIGH/CRITICAL, unfixed) — advarr:ci" >> "$GITHUB_STEP_SUMMARY"
          jq -r '.Results[]? | .Target as $t | .Vulnerabilities[]? | "\(.Severity)\t\(.VulnerabilityID)\t\(.PkgName)\t\(.InstalledVersion)\t\(.FixedVersion // "unfixed")"' \
            /tmp/trivy.json | tee /tmp/findings.txt | sed 's/^/  /' >> "$GITHUB_STEP_SUMMARY" || true
          N=$(wc -l < /tmp/findings.txt)
          if [ "$N" -gt 0 ]; then
            while IFS=$'\t' read -r sev cve pkg inst fixed; do
              echo "::error::Trivy $sev: $cve — $pkg ($inst → $fixed)"
            done < /tmp/findings.txt
            exit 1
          fi
          echo "✅ no HIGH/CRITICAL unfixed findings" >> "$GITHUB_STEP_SUMMARY"

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image (main + tags)
        if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/'))
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
